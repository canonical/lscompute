name: cla-check
on: [pull_request]

permissions:
  pull-requests: read

jobs:
  cla-check:
    runs-on: ubuntu-slim
    steps:
      - name: Check if CLA signed
        uses: canonical/has-signed-canonical-cla@v2
